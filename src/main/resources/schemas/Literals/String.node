description: "A string literal. Raw text."
group: Literals
name: String
extends: Base/Literal

edges:
  value:
    description: 'The string'
    direction: input
    type: string
    value:
      type: string
      default: ""
  result:
    description: 'The string'
    direction: output
    type: string

source: |
  local function ${NODE.name}_${NODE.uid}(value)
    return value
  end